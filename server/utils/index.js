import Auth from './auth';

const {
  checkSignupInputs,
  checkSocialSignupInputs,
  checkSigninInputs,
  authenticate
} = Auth;

export default {
  checkSignupInputs,
  checkSocialSignupInputs,
  checkSigninInputs,
  authenticate
};